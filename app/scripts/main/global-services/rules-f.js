/**
 * @ngdoc service
 * @name cool.Rules
 * @description
 * # Rules
 * Factory in the cool.
 */
angular.module('cool')
    .factory('Rules', function ()
    {
        'use strict';
        var rules = {
            // checks the current State to apply various rules

        };

        return rules;
    });